// Re-exporting or expanding on the home page tech expertise section
// Since we already have a robust one, we will use it as the base or expand it here.
import { TechExpertiseSection } from "@/app/(marketing)/(home)/_components/tech-expertise-section";

export function TechStackGrid() {
    return (
        <TechExpertiseSection />
    );
}
